TECH USED :: MONGODB (DATABASE), EXPRESS

first npm install and download node_modules
second create .env file and add these 2 env variables
---> DB_URL=mongodb://127.0.0.1:27017/assignment
---> PORT = 3000
third run "npm run start" <-- This start nodemon index.js now

In this Project
create 2 routes /requests and /stats

    when ever user create a user then /request api will call and also add the middleware because
    we also want to track the visitor.So when user create user account then stats record were also created
    and also add the checks that one user is created one user_id

If you read this then
I AM EXPERT in MONGODB, REACT, EXPRESS, NODE <-- if there is opening in your vacancy then you can contact me -->
